<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sample.sotuken">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Plan"
            android:label="Plan">
        </activity>
        <activity
            android:name=".Check"
            android:label="Check">
        </activity>
        <activity android:name=".CheckPlanActivity"
            android:label="Check">

        </activity>
        <activity android:name=".CheckPlanSubActivity"
            android:label="Test Check">
        </activity>
        <activity android:name=".CheckPlanDateSort"
            android:label="Check">

        </activity>
        <activity android:name=".CheckPlanDateActivity"
            android:label="Check">

        </activity>
        <activity android:name=".PieChartSample"
            android:label="PieChartSample">

        </activity>
        <receiver android:name=".AlarmBroadcastReceiver"
            android:process=":remote">

        </receiver>
    </application>

</manifest>